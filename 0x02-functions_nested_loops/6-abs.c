#include "main.h"
/**
 * _abs - Turns the negative value to postive
 * @n: Integer value postive or negative
 * Return: Only postive integer
 */
int _abs(int n)
{
	if (n < 0)
		return (-n);

	return (n);
}
