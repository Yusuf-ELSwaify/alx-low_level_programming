#include "main.h"
/**
 * _puts - print the string followed by a new line
 * @str: pointer to array of characters
 */
void _puts(char *str)
{
	int i = 0;

	while (s[i] != '\0')
		_putchar(s[i++]);
	_putchar('\n');
}
